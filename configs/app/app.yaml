app:
  name: "rag-system"
  version: "0.1.0"
  environment: "development"
  debug: false

server:
  host: "0.0.0.0"
  port: 8000
  workers: 1
  timeout_seconds: 60

startup:
  validation_mode: "strict"
  startup_timeout_seconds: 30
